Import('env')

if not env.Bit('linux'):
  Return()

env = env.Clone()

env.Prepend(
    CPPPATH = [
        '$CHROME_SRC_DIR',
    ],
)

input_files = [
    'gtk_clipboard_dump.cc',
]

env.ChromeProgram('gtk_clipboard_dump', input_files)

